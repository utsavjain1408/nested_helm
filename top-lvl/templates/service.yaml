apiVersion: v1
kind: Service
metadata:
  name: {{ include "top-lvl.fullname" . }}
  labels:
    {{- include "top-lvl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "top-lvl.selectorLabels" . | nindent 4 }}
